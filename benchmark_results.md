# wangcai_syn Benchmark 实验记录

## 硬件环境

| 指标 | 数值 |
|------|------|
| CPU | AMD EPYC 7643 48-Core（分配 16 核） |
| 总内存 | 62.8 GB |
| OS | Linux 5.15.0-173-generic (Ubuntu) |
| Python | 3.12 (base conda) |
| wangcai_syn 版本 | 1.3.8 |

---

## 实验1：30 只股票（串行模式）

| 参数 | 值 |
|------|------|
| 股票数量 | 30 只（SH 15, SZ 15） |
| 日期 | 2026-02-27 |
| 策略 | EmptyStrategy（空策略，不下单） |
| 加载方式 | 串行 |
| 转换方式 | 串行 |

### 结果

| 阶段 | 耗时 |
|------|------|
| 数据加载 | 6.79 秒 |
| 回测引擎 | 回测完成（具体秒数未打印，进程析构卡住） |

### 资源占用

| 指标 | 数值 |
|------|------|
| 内存峰值 (RSS) | ~24.3 GB（38.7%） |
| CPU 峰值 | 472%（多线程回测阶段） |

### 备注
- 进程在 `run_backtest` 返回时卡在 C++ engine 析构
- 后通过 `_engine_refs` 持有引擎引用 + `os._exit(0)` 强制退出解决

---

## 实验2：2108 只全量（串行模式，未完成）

| 参数 | 值 |
|------|------|
| 股票数量 | 2108 只（SH 303, SZ 1805），成功 2107，失败 1 只 (300342.SZ) |
| 日期 | 2026-02-27 |
| 策略 | EmptyStrategy |
| 加载方式 | 串行 |
| 转换方式 | 串行 |

### 结果

| 阶段 | 耗时 |
|------|------|
| 数据加载 | 472.68 秒（~7.9 分钟） |
| SH 还原 + CSV 转换 | >12 分钟（手动 kill，未完成） |

### 资源占用

| 指标 | 数值 |
|------|------|
| 加载完成后内存 (RSS) | ~49.7 GB（79.2%） |
| 转换阶段 CPU | 93.4%（单核满载，串行处理） |
| 虚拟内存 | 61 GB |

### 备注
- 内存已达 79%，还未进入回测引擎，预计不够用
- 手动 kill 终止

---

## 实验3：1000 只股票（并行模式，进行中）

| 参数 | 值 |
|------|------|
| 股票数量 | 1000 只（SH 303, SZ 697） |
| 日期 | 2026-02-27 |
| 策略 | EmptyStrategy |
| 加载方式 | 多线程 ThreadPoolExecutor（8 workers） |
| 转换方式 | 多进程 multiprocessing.Pool（8 workers, fork COW） |

### 结果

| 阶段 | 耗时 | 状态 |
|------|------|------|
| 数据加载（8 线程） | 135.93 秒 | ✅ 完成 |
| SH 还原 + CSV 转换（8 进程） | ~3 分钟（估算） | ✅ 完成 |
| 回测引擎初始化 | 进行中... | ⏳ |
| 回测引擎运行 | - | 待定 |

### 资源占用

#### 阶段1：数据加载

| 指标 | 数值 |
|------|------|
| 主进程内存 | ~27 GB（43.1%） |
| 主进程 CPU | 155% |

#### 阶段2：转换数据格式（8 进程 fork COW）

| 指标 | 数值 |
|------|------|
| 主进程内存 (RSS) | ~27 GB（43.1%） |
| 每个 worker 进程 RSS | ~21 GB（COW 共享，非实际占用） |
| 系统已用内存 | ~54.8 GB |
| 系统可用内存 | ~25.2 GB |
| 每个 worker CPU | 85-93%（单核满载） |

#### 阶段3：回测引擎初始化

| 指标 | 数值 |
|------|------|
| 主进程内存 (RSS) | **56.6 GB（85.9%）** |
| 主进程 CPU | 132% |
| 系统可用内存 | **831 MB（极度紧张！）** |

### 最终状态：OOM Killed

进程在 "初始化回测引擎" 阶段被系统 OOM killer 杀掉。
- 死亡时内存：56.6 GB RSS，系统仅剩 831 MB
- 原因：Python DataFrame + C++ SymbolData 双重内存占用，1000 只股票超出 64GB 限制

### 备注
- 转换阶段 8 进程并行，worker 进程通过 fork COW 共享父进程内存
- 转换完成后 worker 进程退出，但 SymbolData (C++ 对象) 留在主进程中
- 引擎初始化阶段内存飙至 56.6GB，系统仅剩 831MB，被 OOM kill

---

## 性能对比

### 数据加载

| 场景 | 耗时 | 加速比 |
|------|------|--------|
| 2108 只 串行 | 472.68s | 基准 |
| 1000 只 8 线程 | 135.93s | ~1.7x（考虑数据量减半后） |

### 内存占用趋势

| 阶段 | 30 只 | 2108 只（串行） | 1000 只（并行） |
|------|-------|----------------|----------------|
| 加载后 | ~24 GB | ~50 GB | ~27 GB |
| 转换中 | - | - | ~55 GB（含 workers） |
| 引擎初始化 | - | - | ~57 GB |

---

## 实验4：600 只股票（并行 + 转换后释放 DataFrame，进行中）

| 参数 | 值 |
|------|------|
| 股票数量 | 600 只（SH 300, SZ 300） |
| 日期 | 2026-02-27 |
| 策略 | EmptyStrategy |
| 加载方式 | 多线程 ThreadPoolExecutor（8 workers） |
| 转换方式 | 多进程 multiprocessing.Pool（8 workers, fork COW） |
| 内存优化 | 转换完成后 data_dict.clear() + gc.collect() 释放 DataFrame |

### 结果

| 阶段 | 耗时 | 状态 |
|------|------|------|
| 数据加载（8 线程） | 82.86 秒 | ✅ 完成 |
| SH 还原 + CSV 转换（8 进程） | 进行中 | ⏳ |
| 回测引擎运行 | - | 待定 |

### 资源占用

#### 阶段2：转换数据格式（8 进程 fork COW，进行中）

| 指标 | 数值 |
|------|------|
| 主进程内存 (RSS) | ~19 GB（30.1%） |
| 主进程 CPU | 147% |
| 每个 worker 进程 RSS | ~13-14 GB（COW 共享） |
| 每个 worker CPU | 88-94% |
| 系统可用内存 | **33 GB** |
| 系统空闲内存 | 24.3 GB |

---

## 实验5：600 只股票（并行 + 释放 DataFrame + 跳过空数据，进行中）

| 参数 | 值 |
|------|------|
| 股票数量 | 600 只（SH 300, SZ 300），成功 598，失败 2 只（000711.SZ, 000793.SZ cstick 为空） |
| 日期 | 2026-02-27 |
| 策略 | EmptyStrategy |
| 加载方式 | 多线程 ThreadPoolExecutor（8 workers） |
| 转换方式 | 多进程 multiprocessing.Pool（8 workers, fork COW） |
| 内存优化 | data_dict.clear() + gc.collect() 释放 DataFrame |

### 结果

| 阶段 | 耗时 | 状态 |
|------|------|------|
| 数据加载（8 线程） | 73.52 秒 | ✅ 完成 |
| SH 还原 + CSV 转换（8 进程） | 完成 | ✅ 完成 |
| 回测引擎初始化 | 进行中... | ⏳ |
| 回测引擎运行 | - | 待定 |

### 资源占用

#### 引擎初始化阶段

| 指标 | 数值 |
|------|------|
| 主进程内存 (RSS) | **57.3 GB（87.1%）** |
| 主进程 CPU | 139% |
| 系统可用内存 | **1.4 GB（极度紧张）** |

### 最终状态：OOM Killed

进程在 "初始化回测引擎" 阶段被 OOM killer 杀掉（与实验3 相同位置）。

| 指标 | 数值 |
|------|------|
| 死亡时内存 (RSS) | 57.3 GB（87.1%） |
| 系统可用内存 | 1.4 GB |

### 备注
- data_dict.clear() + gc.collect() 未明显降低 RSS，Python 内存分配器不主动归还 OS
- 原因：SymbolData (C++) 持有全部 CSV 字符串 + 引擎初始化解析 CSV 产生额外内存
- DataFrame → CSV string → C++ 解析，三阶段内存重叠
- 600 只的内存占用与 1000 只几乎相同（57.3 vs 56.6 GB），说明瓶颈不完全在股票数量

---

## 实验6：600 只（并行 + C++ 逐只释放 CSV + Python 逐只释放 DataFrame，进行中）

| 参数 | 值 |
|------|------|
| 股票数量 | 600 只（SH 300, SZ 300），成功 598，失败 2 只 |
| 日期 | 2026-02-27 |
| 策略 | EmptyStrategy |
| 加载方式 | 多线程 ThreadPoolExecutor（8 workers） |
| 转换方式 | 多进程 multiprocessing.Pool（8 workers） |
| 内存优化 | C++: 构造函数 by value，逐只 clear+shrink_to_fit CSV 字符串；Python: 逐只 pop DataFrame |

### 结果

| 阶段 | 耗时 | 状态 |
|------|------|------|
| 数据加载（8 线程） | 80.18 秒 | ✅ 完成 |
| SH 还原 + CSV 转换（8 进程） | 完成 | ✅ 完成 |
| 回测引擎初始化 | 进行中... | ⏳ |

### 资源占用

#### 引擎初始化阶段（进行中）

| 指标 | 数值 | 对比实验5 |
|------|------|----------|
| 主进程内存 (RSS) | **31.7 GB（50.4%）** | 57.3 GB（87.1%）|
| 主进程 CPU | 129% | 139% |
| 系统可用内存 | **23.4 GB** | 1.4 GB |

### 效果对比（同阶段：引擎初始化中）

| 指标 | 实验5（无释放） | 实验6（逐只释放） | 改善 |
|------|----------------|------------------|------|
| 进程内存 | 57.3 GB | 31.7 GB | **-25.6 GB（-44.7%）** |
| 系统可用 | 1.4 GB | 23.4 GB | **+22 GB** |

---

## 关键发现

1. **内存是主要瓶颈**：1000 只股票在引擎初始化阶段已达 57GB/63GB，全量 2108 只不可能在 64GB 机器上跑通
2. **数据加载多线程效果有限**：I/O 受磁盘带宽约束，8 线程仅提升 ~1.7x
3. **SH 订单还原 + to_csv 多进程有效**：8 进程并行将原本 12+ 分钟的串行操作压缩到 ~3 分钟
4. **DataFrame → CSV 字符串 → C++ 解析的架构导致双重内存占用**：Python DataFrame + C++ SymbolData 同时存在于内存中
5. **C++ engine 析构会卡住进程**：需要通过 `_engine_refs` + `os._exit(0)` 绕过
