# 严格主动单模式 (Strict Active Order Mode)

## 1. 功能总结
新增**严格主动单模式**，用于更真实地模拟市场撮合：

- **欠债限制**：虚拟主动单吃掉的历史订单，在被真实市场消耗前，禁止下新的主动单
- **被动单不受影响**：挂单（价格不穿越对手盘）可正常提交
- **可配置开关**：默认关闭

---
## 2. 使用方法
```python
from wangcai_syn import run_backtest

# 启用严格主动单模式
run_backtest(
    data_dict=data,
    strategy=my_strategy,
    strict_active_order_mode=True  # 开启严格模式
)
```

参数说明：
- `strict_active_order_mode=True`：开启严格模式，主动单受欠债限制
- `strict_active_order_mode=False`（默认）：关闭严格模式，主动单无限制
---

## 3. 实现逻辑

### 欠债记录
当虚拟主动单提交时，引擎会遍历对手盘，找出会被"吃掉"的历史订单，并将这些订单ID记录到一个"欠债集合"中。

### 欠债检查
后续提交的主动单会先检查欠债集合是否为空：
- 非空 → 拒绝订单，触发撤单回调
- 为空 → 正常处理

### 欠债清除
当历史订单被真实市场成交（而非虚拟成交）时，从欠债集合中移除对应订单ID。全部清除后，新的主动单可正常提交。

### 被动单不受影响
只有价格穿越对手盘的主动单才会触发欠债检查，被动挂单不受限制。
