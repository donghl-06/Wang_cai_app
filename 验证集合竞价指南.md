# 集合竞价验证指南

## 简介
这个验证程序用于测试我们的集合竞价算法的准确性，通过读取真实的市场数据（csord订单、cstra撤单、cstick价格）来验证算法计算的开盘价是否与实际开盘价一致。

## 文件结构
- `src/call_auction_validation.cpp` - 主验证程序
- `src/OrderLoader.cpp` - 数据加载器，包含验证函数
- `include/OrderLoader.h` - 头文件

## 编译运行

### 1. 编译项目
```bash
mkdir build
cd build
cmake ..
make call_auction_validation
```

### 2. 准备数据文件
确保在 `logs/` 目录下有以下格式的文件：
- `csord_[股票代码]_[日期].csv` - 订单数据
- `cstra_[股票代码]_[日期].csv` - 撤单数据  
- `cstick_[股票代码]_[日期].csv` - 价格数据

例如：
- `logs/csord_600236.SH_2022-01-07.csv`
- `logs/cstra_600236.SH_2022-01-07.csv`
- `logs/cstick_600236.SH_2022-01-07.csv`

### 3. 运行验证
```bash
./call_auction_validation
```

## 数据文件格式

### csord文件（订单数据）
```
datetime,sym,price,size,side,ordertype,orderid,channelno,seqno,bizindex
09:15:00.123,600236.SH,6.03,1000,1,0,12345,1,100,1001
```

### cstra文件（撤单数据）
```
datetime,sym,price,size,bidorderid,askorderid,tradeid,exectype,tradebsflag,channelno,bizindex
09:20:00.456,600236.SH,6.03,500,12345,0,0,2,B,1,1002
```

### cstick文件（价格数据）
```
datetime,sym,prevclose,open,high,low,close,...
09:25:00.000,600236.SH,6.03,6.05,6.10,6.00,6.04,...
```

## 验证流程

1. **读取数据**：从cstick文件读取前收盘价和实际开盘价
2. **初始化**：创建OrderBook和CallAuctionEngine
3. **处理订单**：按bizindex时间顺序处理09:15-09:25的订单和撤单
4. **计算结果**：获取集合竞价预测的开盘价和成交量
5. **对比验证**：比较预测开盘价与实际开盘价

## 验证结果

程序会输出：
- ✅ 验证成功：预测价格与实际开盘价完全一致
- ❌ 验证失败：显示预测价格与实际开盘价的差异百分比

## 添加更多测试用例

在 `src/call_auction_validation.cpp` 中修改 `test_cases` 数组：
```cpp
vector<pair<string, string>> test_cases = {
    {"600236.SH", "2022-01-07"},
    {"000001.SZ", "2022-01-10"},  // 添加更多股票
    // ... 更多测试用例
};
```

## 注意事项

1. **价格对齐**：程序会自动将价格对齐到tick（股票=0.01元，ETF=0.001元）
2. **时间过滤**：只处理09:15-09:25的订单和撤单
3. **异常处理**：超出涨跌停范围的订单会被跳过
4. **撤单逻辑**：根据tradebsflag判断是买单还是卖单的撤销

## 调试建议

如果验证结果不一致，可以：
1. 检查数据文件格式是否正确
2. 确认时间过滤逻辑（09:15-09:25）
3. 验证价格对齐计算
4. 比较撤单处理逻辑 